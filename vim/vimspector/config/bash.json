{
  "configurations": {
    "Bash - Launch": {
      "adapter": "vscode-bash",
      "configuration": {
        "program": "${file}",
        "args": [
          "*${CommandLineArgs}"
        ]
      }
    }
  }
}
